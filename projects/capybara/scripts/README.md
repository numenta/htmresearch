Scripts to run experiments. Usage: `./run-all-exps.sh <target_dir>` where 
`<target_dir>` is the path to the folder where you want the experiments results to be saved. 

For example: `./run-all-exps.sh ~/Desktop`
